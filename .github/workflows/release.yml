name: release

on:
  workflow_dispatch:
    inputs:
      package:
        description: "Package to publish"
        required: true
        type: choice
        default: pi-quicksave
        options:
          - pi-quicksave
          - pi-rename-session
          - pi-hud
      bump:
        description: "Semver bump to apply before publishing"
        required: false
        type: choice
        default: patch
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  pull-requests: write
  id-token: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          package-manager-cache: false
      - name: Run checks
        working-directory: ${{ github.event.inputs.package }}
        run: |
          npm ci
          npm run check
          npm test
      - name: Bump version
        working-directory: ${{ github.event.inputs.package }}
        run: npm version ${{ github.event.inputs.bump }} --no-git-tag-version
      - name: Publish to npm
        working-directory: ${{ github.event.inputs.package }}
        run: npm publish --provenance --access public
      - name: Commit version bump and merge to main
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          pkg="${{ github.event.inputs.package }}"
          ver="$(node -p "require('./$pkg/package.json').version")"
          branch="chore/release-$pkg-$ver"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$pkg/package.json"
          [ -f "$pkg/package-lock.json" ] && git add "$pkg/package-lock.json"
          git commit -m "chore(release): publish $pkg@$ver"
          git switch -c "$branch"
          git push -u origin "$branch"
          pr="$(gh pr create --base main --head "$branch" --title "chore(release): publish $pkg@$ver" --body "Auto-generated by the release workflow." --json number -q .number)"
          gh pr checks "$pr" --watch --interval 10 >/dev/null
          gh pr merge "$pr" --squash --delete-branch
